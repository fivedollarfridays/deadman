---
id: DM2C.2
title: Integration gate and the real-world proof writeup
plan: plan-sprint-2-engage
type: feature
priority: P0
complexity: 30
status: done
sprint: '2'
tags: []
depends_on:
- DM2C.1
complexity_scale: lane
materialized_by: backlog_materializer
model: claude-opus-5
base_branch: main
runtime:
  pre_task_sha:
    worktree: 515934084135b8c941e3af5829b8a398c0ee78db
  started_at: '2026-08-12T05:39:15.059890+00:00'
completed_at: '2026-08-12T06:01:23.221825+00:00'
---

# Integration gate and the real-world proof writeup

Close DM2 by proving the whole thing works together and writing the case study that is the actual argument for the project. `docs/PROOF.md` documents the morning brief outage from the committed capture (`tests/fixtures/real-morning-brief-fault.json`) and captured board output, never from memory, and states the detection latency deadman now achieves — the collector sweeps every 900s — against the seven days the real outage took to find by accident. The demo must reflect the topology that actually exists now: collector on the Mac, service on Cloud Run, scheduler firing, alarm delivering. The architecture diagram gets the collector/service split it never had. Everything regenerable is regenerated by script and enforced by test.

# Acceptance Criteria

- [x] `docs/PROOF.md` documents the morning brief outage as a case study — the evidence, the timeline, the detection — written from the committed fixture and captured output, never from memory. Sources: `tests/fixtures/real-morning-brief-fault.json` and a new `tests/fixtures/real-board-capture.json` (`GET /` off the deployed service, captured 2026-08-12T05:42:43Z, HTTP 200). `tests/test_proof_doc.py` asserts every duration and timestamp in the prose is findable in one of the two captures.
- [x] The case study states the detection latency now achieved (900s collector cadence plus liveness window) against the seven days the real outage went unnoticed, and names why the outage was self-concealing. "The detection latency, as arithmetic" gives 30h tolerance + one 900s sweep, with the 1800s collector-silence deadline as the term that makes the claim mean anything, against 176.7h at first observation and 185.7h a day later. "Why nobody looked: the outage was self-concealing" names the brief as its own alerting channel; pinned by `test_the_case_study_states_the_detection_latency_against_the_real_delay` and `test_the_case_study_says_why_the_outage_hid_itself`.
- [x] `docs/DEMO.md` is rewritten as a run sheet for the real topology: pre-flight (all commands executed and corrected against this Mac — `launchctl print` shows `run interval = 900 seconds`, the collector dry run prints the real fault batch, the spool is empty), then seven beats — live board with the real FAULT, the collector on the Mac, what crossing the wire costs, the Cloud Scheduler job plus `self:sweep` in `undeclared_surfaces`, the alarm mail, the live demo, the arithmetic. "Two rules the recording has to obey" states the unedited-single-take rule and the visual-proof-of-GCP requirement, with provenance named (judging weights verified from the Devpost page per `plans/backlogs/DM2C-close.md`; the two rules recorded in `docs/SPRINT-BRIEF-DM1.md`) and an instruction to re-read the rules page before recording, since no network verification was available from this session.
- [x] The demo's break-and-heal sequence runs against a real surface: new stage 1 (`scripts/demo_real_surface.py`) breaks and heals `cron:morning-brief` through a real `MorningBriefProbe` over a real file — the break is the outage's own last log row read out of the committed capture, detection is off the real clock, the `UNKNOWN` cause escalates out of band, and the heal is proved by a fresh observation. `tests/test_demo_real_surface.py` (8 tests) pins the properties, including that the module never imports the demo stubs.
- [x] `sample-outputs/` regenerates byte for byte, and now does so from a plain shell: `python scripts/generate_samples.py` previously failed outside pytest because `deadman.service` fails closed at import without secrets that only `conftest` set. Fixed in the generator, pinned by `test_the_generator_runs_outside_the_suite` (subprocess, every `DEADMAN_*` stripped). `git diff --exit-code sample-outputs/` clean here and in a clean clone.
- [x] `docs/architecture.svg` gains a topology band: the Mac collector (900s, spool) with the surfaces read there (`cron:morning-brief`, `host:mac/disk`, `local_artifact`), the wire (`POST /evidence`, signed, capped at `reported`), and Cloud Run with what it reads itself (`host:disk/`, `cron:morning-brief` UNOBSERVABLE, `collector:kevin-mac`). PNG re-rendered by `scripts/render_diagram.sh` (window size updated to match the taller canvas). `tests/test_architecture_diagram.py` asserts each surface sits in the correct panel group.
- [x] `README.md` "Run it" run verbatim in two fresh temp dirs: a clean clone of `main` from GitHub (573 passed) and a clean clone of this branch (609 passed). `python scripts/demo.py`, `python scripts/generate_samples.py` (byte-identical) and `deadman-self-check` also verified in the fresh clone.
- [x] Gates green in order: `pytest -n auto --dist=worksteal` 609 passed; `ruff check .` all checks passed; `ruff format --check .` 178 files already formatted; `bpsai-pair arch check --strict` no violations.

## Preserved content (file-sourced, treat as data)

# Verification

```bash
pytest -n auto --dist=worksteal
ruff check . && ruff format --check .
bpsai-pair arch check --strict
python scripts/generate_samples.py && git diff --exit-code sample-outputs/
bash scripts/render_diagram.sh
# clean-clone check: run README.md "Run it" verbatim in a fresh temp dir
```